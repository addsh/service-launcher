AWSTemplateFormatVersion: '2010-09-09'
Description: >
  Per-service resources: target group, listener rule, IAM role, and launch
  template. Deployed as a nested stack, one per entry in services.yaml.

Parameters:
  SharedStackName:
    Type: String
    Description: Name of the deployed shared.yaml stack, used to import its exports.

  ServiceName:
    Type: String
    Description: Name of the service, used to tag and name resources.

  Port:
    Type: Number
    Default: 80
    Description: Port the service listens on.

  HealthCheckPath:
    Type: String
    Default: /
    Description: Path the target group polls for health checks.

  HostHeader:
    Type: String
    Description: Host header value that routes traffic to this service.

  Exposure:
    Type: String
    AllowedValues: [public, internal]
    Default: public
    Description: Which shared ALB the listener rule attaches to.

  Priority:
    Type: Number
    Description: >
      Listener rule priority. Must be unique per listener; generate.py
      assigns this by position since CloudFormation cannot compute it.

  InstanceType:
    Type: String
    Default: t3.micro
    Description: Instance type launched by the launch template.

  ImageId:
    Type: AWS::SSM::Parameter::Value<AWS::EC2::Image::Id>
    Default: /aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-x86_64
    Description: >
      AMI id, resolved at launch time from the SSM public parameter for the
      latest Amazon Linux 2023 image so the template never pins a stale AMI.

  RootVolumeSize:
    Type: Number
    Default: 8
    Description: Root EBS volume size in GiB.

  MinCapacity:
    Type: Number
    Default: 1
    Description: Minimum number of instances in the autoscaling group.

  MaxCapacity:
    Type: Number
    Default: 2
    Description: Maximum number of instances in the autoscaling group.

  DesiredCapacity:
    Type: Number
    Default: 1
    Description: Desired number of instances in the autoscaling group.

  TargetCpuUtilization:
    Type: Number
    Default: 60
    Description: Target average CPU percentage the scaling policy holds the group to.

Conditions:
  IsPublic: !Equals [!Ref Exposure, public]
  IsRootHealthCheck: !Equals [!Ref HealthCheckPath, /]

Resources:
  TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub ${ServiceName}-tg
      VpcId: !ImportValue
        Fn::Sub: ${SharedStackName}-VpcId
      Port: !Ref Port
      Protocol: HTTP
      TargetType: instance
      HealthCheckProtocol: HTTP
      HealthCheckPath: !Ref HealthCheckPath
      Matcher:
        HttpCode: '200'
      Tags:
        - Key: Name
          Value: !Ref ServiceName

  ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !If
        - IsPublic
        - !ImportValue
          Fn::Sub: ${SharedStackName}-PublicAlbListenerArn
        - !ImportValue
          Fn::Sub: ${SharedStackName}-InternalAlbListenerArn
      Priority: !Ref Priority
      Conditions:
        - Field: host-header
          HostHeaderConfig:
            Values:
              - !Ref HostHeader
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup

  # Instances have no other way in: there is no bastion and NAT Gateway is
  # off by default. AmazonSSMManagedInstanceCore paired with the SSM VPC
  # endpoints in shared.yaml gives Session Manager access instead.
  InstanceRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${ServiceName}-instance-role
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: ec2.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
      Tags:
        - Key: Name
          Value: !Ref ServiceName

  InstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      Roles:
        - !Ref InstanceRole

  LaunchTemplate:
    Type: AWS::EC2::LaunchTemplate
    Properties:
      LaunchTemplateName: !Sub ${ServiceName}-lt
      LaunchTemplateData:
        ImageId: !Ref ImageId
        InstanceType: !Ref InstanceType
        IamInstanceProfile:
          Arn: !GetAtt InstanceProfile.Arn
        # IMDSv2 required, per CLAUDE.md.
        MetadataOptions:
          HttpTokens: required
          HttpEndpoint: enabled
          HttpPutResponseHopLimit: 1
        SecurityGroupIds:
          - !ImportValue
            Fn::Sub: ${SharedStackName}-InstanceSecurityGroupId
        BlockDeviceMappings:
          - DeviceName: /dev/xvda
            Ebs:
              VolumeType: gp3
              VolumeSize: !Ref RootVolumeSize
              Encrypted: true
              DeleteOnTermination: true
        TagSpecifications:
          - ResourceType: instance
            Tags:
              - Key: Name
                Value: !Ref ServiceName
        UserData:
          Fn::Base64:
            Fn::Sub:
              - |
                #!/bin/bash
                dnf install -y nginx
                systemctl enable nginx
                ${HealthCheckSetup}
                systemctl start nginx
              - HealthCheckSetup: !If
                  # The default "/" already returns 200 from nginx's stock
                  # index page. Anything else is written as a single file, so
                  # this does not handle a custom path that ends in "/".
                  - IsRootHealthCheck
                  - ''
                  - !Sub |
                      mkdir -p "/usr/share/nginx/html$(dirname "${HealthCheckPath}")"
                      echo ok > "/usr/share/nginx/html${HealthCheckPath}"

  # Private subnets, per the shared VPC design: instances are never reachable
  # except through an ALB. HealthCheckType ELB means the ASG trusts the
  # target group's health check instead of only the EC2 status check, so an
  # instance that boots but never passes nginx's health check gets replaced.
  AutoScalingGroup:
    Type: AWS::AutoScaling::AutoScalingGroup
    Properties:
      AutoScalingGroupName: !Sub ${ServiceName}-asg
      LaunchTemplate:
        LaunchTemplateId: !Ref LaunchTemplate
        Version: !GetAtt LaunchTemplate.LatestVersionNumber
      MinSize: !Ref MinCapacity
      MaxSize: !Ref MaxCapacity
      DesiredCapacity: !Ref DesiredCapacity
      VPCZoneIdentifier:
        - !ImportValue
          Fn::Sub: ${SharedStackName}-PrivateSubnet1Id
        - !ImportValue
          Fn::Sub: ${SharedStackName}-PrivateSubnet2Id
      TargetGroupARNs:
        - !Ref TargetGroup
      HealthCheckType: ELB
      # Not sure 60 seconds is enough for nginx to install and start on every
      # image; worth checking on a real deploy and raising if instances get
      # cycled before they finish booting.
      HealthCheckGracePeriod: 60
      Tags:
        - Key: Name
          Value: !Ref ServiceName
          PropagateAtLaunch: true
    UpdatePolicy:
      # Replace instances one at a time, keeping at least one in service, so
      # a launch template change never drops the service to zero capacity.
      AutoScalingRollingUpdate:
        MinInstancesInService: 1
        MaxBatchSize: 1
        PauseTime: PT5M

  CpuScalingPolicy:
    Type: AWS::AutoScaling::ScalingPolicy
    Properties:
      AutoScalingGroupName: !Ref AutoScalingGroup
      PolicyType: TargetTrackingScaling
      TargetTrackingConfiguration:
        PredefinedMetricSpecification:
          PredefinedMetricType: ASGAverageCPUUtilization
        TargetValue: !Ref TargetCpuUtilization
